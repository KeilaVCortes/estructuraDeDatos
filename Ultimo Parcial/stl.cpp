/*
    usar la libreria standar
 */


#include<iostream>
#include<vector>
#include <algorithm>

using namespace std;
/*
int main(){
    vector<int> numeros;
    numeros.push_back(1);
    numeros.push_back(2);
    
    for(auto i: numeros){
        cout << i << endl;
    }
    copy(numeros.beguin(), numeros.end(), ostream_iterator<int>(cout));
    
    
    return 0;
}

*/